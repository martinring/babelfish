{
  "types": {
    "casing": "pascal",
    "sequences": "Seq[$elem$]",
    "mappings": {
      "int": "Int",
      "string": "String",
      "bool": "Boolean"
    },
    "files-per-type": false,
    "files": {
      "$options.base$$for(options.module)$$options.module$/$endfor$$name.pascal$.scala": { "import": "scalajs/types.scala" }
    }
  },
  "api": {        
    "files": {
      "$options.base$$for(options.module)$$options.module$/$endfor$$api.name.pascal$.scala": { "import": "scalajs/api.scala" }
    }
  }
}
