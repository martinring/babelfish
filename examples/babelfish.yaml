messages:
  configs:
    haskell:      
      api:
        server: local
        client: remote
      options:
        base: "app/"
        module: [Example]
    scalajs:
      api:
        server: remote
        client: local
      options:
        base: "ui/src/main/scala/"
        module: [de, dfki]